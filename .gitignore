# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# Tooling
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/

# OS
.DS_Store

# Machine-local agent settings. Covered by many users' global gitignore, but
# repeated here so the protection travels with the repository.
.claude/settings.local.json

# Note: papers/figures/*.png and explainer/figures/*.png ARE committed.
# They are referenced by README.md and explainer/05-the-evidence.md, and are
# regenerable via `python -m retainiq.experiments.figures`.

# Benchmark datasets are downloaded on demand (retainiq/benchmarks/datasets.py).
# Not committed: multi-MB, and redistribution terms vary by source.
data/

# Editor workspace config (Obsidian vault settings, machine-local)
.obsidian/

# Generated dashboards (reproducible via `make dashboard`)
retention_dashboard.html
sample_churn_autopsy.html
sample_*.csv
churn_autopsy.html
*_departures.csv
*_unrecovered_failed_payments.csv

# Zenodo data release (regenerable via `python -m retainiq.experiments.zenodo_release`)
zenodo_release/
RetainIQ_SubSim_datasets.zip
